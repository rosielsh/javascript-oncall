import { Console } from "@woowacourse/mission-utils";

class OutputView {
  static printEmergencySchedule(schedules) {
    // 월, 일, 휴일 여부, 근무자 출력
    Console.print();
  }
}

export default OutputView;
